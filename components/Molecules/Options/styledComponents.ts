import { styled } from "@mui/material/styles";
import { Box } from "@mui/material";

export const Container = styled(Box)(() => ({
  position: "relative",
  width: "150px",
  height: "32px",
  display: "flex",
  gap: "20px",
  overflow: "visible",
}));
